#ifndef DF_MATH_TRANSFORM_D
#define DF_MATH_TRANSFORM_D

#include "core/types.h"

// Represents a transform, containing both separate components and the
// resulting transformation matrix.
declarep(struct, transform);

#endif
